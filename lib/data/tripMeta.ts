// Trip-wide settings that don't fit any other data file. The app never
// hosts or uploads photos itself — this just links out to wherever you're
// already sharing them with family (a shared Google Photos or iCloud
// album). Add the link once you've created one.
export const tripMeta = {
  sharedAlbumUrl: undefined as string | undefined,
};
